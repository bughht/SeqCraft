"""
Experimental module-mining tooling.  **Not part of SeqCraft's public API.**

Nothing under this package is imported by ``seqcraft``; it exists to make external sequence
implementations measurable so that a candidate abstraction can be argued from numbers rather than
from source-code similarity.  Paths, names and return shapes here are free to change.

See ``docs/plans/module-mining/`` in the workspace repository for the plan this serves.
"""
