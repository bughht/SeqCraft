# Handover — SeqCraft Module Mining Next Phase

> **Mirrored from the workspace planning repository** (`docs/plans/module-mining/`) on
> 2026-09-18, so that the pull request extracting `TSEShot` and `FSE2D` carries the evidence
> that justified it.  The two copies are identical today and there is nothing keeping them
> that way; see [`../README.md`](../README.md) for which one to edit.

TSE/FSE is now considered **COMPLETE — GREEN**.

Immediate next steps:

1. Update the planning docs so the TSE/FSE pilot is marked complete. Keep the earlier YELLOW sweep report as historical evidence, but make clear that the later `boundary_decision.md` supersedes it after human ownership review and extraction.

2. Start a new **RadialReadout fine scan**. Do not implement the class first.

3. Primary references:
   - official PyPulseq radial GRE;
   - Pulseq MATLAB radial GRE;
   - one independent radial implementation.
   Python remains the package implementation target; MATLAB is reference/validation evidence only.

4. Main questions:
   - full spoke vs centre-out: one abstraction or two?
   - does the leaf own angle/orientation?
   - does the leaf own semantic `k=0` timing/location?
   - keep spoke ordering / golden-angle schedule / scan policy above the leaf.

5. Build the invariant table before the API:
   trajectory angle, k-space extent/Δk, ADC-gradient synchronization, semantic `k=0`, gradient/slew legality, full-spoke symmetry when applicable.

6. Add a key metamorphic check:
   `RadialReadout(angle=φ) ≈ rotate(RadialReadout(angle=0), φ)`.

7. Do **not** create `seqcraft/trajectory/` yet. Revisit that only after Radial + Spiral reveal the same stable numerical-design contract.

8. If Radial becomes GREEN, compare the TSE and Radial pilots before formalizing `ModuleCandidate`, `ReferenceSequence`, `SequenceFingerprint`, or module-miner skill APIs.

Recommended candidate order afterward:

```text
GRE3DTR → SaturationPrep → DiffusionPrep/Encoding → SpiralReadout
```

Continue using the ownership rule established by TSE:

```text
leaf = intrinsic physics/geometry
kernel = coupling between leaves
imaging = acquisition policy
compiler = Pulseq legality
```
